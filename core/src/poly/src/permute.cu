// #include "permute.cuh"
// #include "poly.h"

// cudaError_t permute(void *temp_buffer, unsigned long *buffer_size, unsigned long usable, ConstPolyPtr input, ConstPolyPtr table, PolyPtr res_input, PolyPtr res_table, cudaStream_t stream) {
//     return detail::permute<POLY_FIELD>(temp_buffer, buffer_size, usable, input, table, res_input, res_table, stream);
// }