[package]
name = "zkpoly_compiler"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
zkpoly_runtime = { path = "../runtime" }
zkpoly_common = { path = "../common" }
